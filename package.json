{
  "name": "knowledge-map",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "sync:functions": "rm -rf functions && cp -R api functions",
    "deploy:pages": "npm run sync:functions && npm run build -w apps/k-maps && npx wrangler pages deploy apps/k-maps/dist/k-maps/browser --project-name k-maps --branch main",
    "deploy:ionic": "npm run build -w apps/app-k-maps && npx wrangler pages deploy apps/app-k-maps/www --project-name app-k-maps --branch main",
    "deploy:all": "npm run deploy:pages && npm run deploy:ionic"
  },
  "dependencies": {
    "primeflex": "^4.0.0",
    "primeicons": "^7.0.0",
    "primereact": "^10.9.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260109.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.42.2"
  }
}
