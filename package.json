{
  "name": "knowledge-map",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "wrangler pages dev apps/k-maps/dist/k-maps/browser --port 8788 --functions-dir functions --remote",
    "deploy:pages": "npm run build -w apps/k-maps && ./scripts/deploy-pages-with-api.sh \"$(pwd)/apps/k-maps/dist/k-maps/browser\" k-maps main",
    "deploy:ionic": "npm run build -w apps/app-k-maps && npx wrangler pages deploy apps/app-k-maps/www --project-name app-k-maps --branch main",
    "deploy:all": "npm run deploy:pages && npm run deploy:ionic"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260120.0",
    "pdfjs-dist": "^5.4.624",
    "typescript": "^5.9.3",
    "wrangler": "^4.42.2"
  }
}
