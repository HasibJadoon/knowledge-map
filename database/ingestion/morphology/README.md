# Morphology Ingestion

Expected QUL source files:

- `sources/qul/word-lemma.sqlite`
- `sources/qul/word-root.sqlite`
- `sources/qul/word-pos.csv`

Intermediate transforms belong in `staging/`. Generated SQL/JSON outputs belong in `outputs/`.
