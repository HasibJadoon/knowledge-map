# Root Ingestion

Expected source files:

- `sources/tarteel-ai/allroots.sql` - raw Tarteel root export.

Generated files:

- `outputs/roots-only.sql` - normalized root-only SQL used by root import/regeneration scripts.

Staging files belong in `staging/`.
