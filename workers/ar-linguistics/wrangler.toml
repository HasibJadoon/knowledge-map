name = "km-ar-linguistics-worker"
main = "src/index.ts"
compatibility_date = "2025-01-01"
workers_dev = false   # internal only — accessed via km-backend-worker service binding

[[d1_databases]]
binding = "DB_AL"
database_name = "km_arabic_linguistic"
database_id = "192f4792-deb3-4f2e-9204-532cf484ecd2"
migrations_dir = "migrations"
# NOTE: All tables use single prefix ar_ling_* (no al_* prefix anywhere)
# LX:... typed refs resolve to DB_AL for backward compat

[[services]]
binding = "CONTENT"
service = "km-content-worker"
