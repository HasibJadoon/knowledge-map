export const environment = {
  production: false,
  // Development uses the Angular proxy so API calls stay same-origin while
  // forwarding to the hosted backend. No local API worker is required.
  apiBase: '/api',
};
